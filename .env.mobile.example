# Exemple de variables d'environnement pour l'application mobile (Expo)

API_URL=http://192.168.1.100:3000

# Note: Ici on pourrait ajouter d'autres variables spécifiques à l'app mobile si nécessaire (ex: Google API keys, etc.)
# La variable LOCAL_IP n'est pas utilisée directement par l'app, on l'emploie juste pour composer API_URL manuellement.
